# MYSQL Settings
quarkus.datasource.db-kind=mysql
quarkus.datasource.reactive.url=${DB_URL}
quarkus.datasource.username=${DB_USERNAME}
quarkus.datasource.password=${DB_PASSWORD}
quarkus.datasource.reactive.max-size=7
quarkus.datasource.reactive.idle-removal-interval=PT30S

# Connection timeout settings
quarkus.datasource.reactive.connect-timeout=600
quarkus.datasource.reactive.idle-timeout=PT30S
quarkus.datasource.reactive.max-lifetime=PT30S
quarkus.thread-pool.max-threads=10

# CRITICAL FIX: Bind to Railway PORT environment variable
quarkus.http.host=0.0.0.0
quarkus.http.port=${PORT:8088}

organization-api/mp-rest/url=http://localhost:8080
%prod.organization-api/mp-rest/url=${ORG_SERVICE_URL:http://ai-solutions-organization-api.railway.internal:8080}
%staging.organization-api/mp-rest/url=${ORG_SERVICE_URL}

# Hibernate/JPA - Enable auto DDL generation
quarkus.hibernate-orm.database.generation=update

# Disable Kafka to reduce noise

# FTP Settings (optional - set to empty if not used)
ftp.host=${FTP_HOST:ftp.example.com}
ftp.username=${FTP_USERNAME:ftpuser}
ftp.password=${FTP_PASSWORD:ftppass}

# CORS Settings
quarkus.http.cors.enabled=true
quarkus.http.cors.origins=http://localhost:4000,http://localhost:4200,http://localhost:4003
quarkus.http.cors.methods=GET,PUT,POST,DELETE,PATCH,OPTIONS
quarkus.http.cors.headers=X-Custom,Content-Type,Authorization,authorization
quarkus.http.cors.exposed-headers=Content-Disposition,Authorization
quarkus.http.cors.access-control-max-age=24H
quarkus.http.cors.access-control-allow-credentials=true
%prod.quarkus.http.cors.origins=https://ai-solutions-shell-production.up.railway.app,https://aisolutionspl.com,https://aisolutionspl.up.railway.app
